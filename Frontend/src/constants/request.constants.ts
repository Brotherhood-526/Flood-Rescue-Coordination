// Các key dùng để lưu vào localStorage
export const RESCUE_STORAGE_KEYS = {
  REQUEST_ID: "rescue_requestId",
  PHONE: "rescue_phone",
} as const;

// Tọa độ mặc định khi mở bản đồ TP.HCM
export const HCMC_CENTER: [number, number] = [106.7009, 10.7769];
export const MAX_UPLOAD_IMAGES = 3;
